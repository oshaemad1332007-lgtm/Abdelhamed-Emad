#include <stdio.h>

int main()
{
    int length, wigth;
    printf("please enter your length and wigth : \n");
    scanf("%d%d", &length, &wigth);

    printf("the area is = %d", length * wigth);

    return 0;
}
