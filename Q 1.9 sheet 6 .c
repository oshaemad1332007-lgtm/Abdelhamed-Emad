#include <stdio.h>
#include <stdio.h>
void sum(int a,int b){

printf("Sum of squares of  %d and %d  is  %d \n",a,b,((a*a)+(b*b)));

}

int main() {
int a,b;
scanf("%d%d",&a,&b);
sum(a,b);

    return 0;
}
